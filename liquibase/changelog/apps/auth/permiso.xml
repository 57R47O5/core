<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <changeSet
        id="auth-004-create-permiso"
        author="orco">

        <createTable tableName="permiso">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <!-- Identidad lógica -->
            <column name="code" type="VARCHAR(150)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="VARCHAR(255)"/>

            <column name="app_label" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <!-- Estado -->
            <column name="is_deleted" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <!-- Auditoría -->
            <column name="createdat" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="updatedat" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="createdby_id" type="BIGINT"/>
            <column name="updatedby_id" type="BIGINT"/>

        </createTable>

        <!-- Índices útiles -->
        <createIndex tableName="permiso" indexName="idx_permiso_code">
            <column name="code"/>
        </createIndex>

        <createIndex tableName="permiso" indexName="idx_permiso_app">
            <column name="app_label"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
