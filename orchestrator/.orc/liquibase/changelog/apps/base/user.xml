<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <changeSet id="base-001-create-user-table" author="esteban">

        <createTable tableName="auth_user">

            <!-- ================= -->
            <!-- BaseModel fields -->
            <!-- ================= -->

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="is_deleted" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="createdby_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>

            <column name="updatedby_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>

            <column name="createdat" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="updatedat" type="TIMESTAMP WITH TIME ZONE"/>

            <!-- ================= -->
            <!-- AbstractUser fields -->
            <!-- ================= -->

            <column name="password" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>

            <column name="last_login" type="TIMESTAMP WITH TIME ZONE"/>

            <column name="is_superuser" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="username" type="VARCHAR(150)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="first_name" type="VARCHAR(150)"/>
            <column name="last_name" type="VARCHAR(150)"/>

            <column name="email" type="VARCHAR(254)"/>

            <column name="is_staff" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

            <column name="date_joined" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- Indexes -->
        <createIndex indexName="idx_user_username" tableName="auth_user">
            <column name="username"/>
        </createIndex>

        <createIndex indexName="idx_user_email" tableName="auth_user">
            <column name="email"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
