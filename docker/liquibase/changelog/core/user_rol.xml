<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <!-- ===================================================== -->
    <!-- Tabla: user_rol -->
    <!-- ===================================================== -->

    <changeSet id="user-rol-001-create-table" author="system">
        <createTable tableName="user_rol">
            <column name="id" type="SERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="rol_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="is_deleted" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="createdby_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="updatedby_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="createdat" type="TIMESTAMP"/>
            <column name="updatedat" type="TIMESTAMP"/>
        </createTable>
    </changeSet>

    <!-- ===================================================== -->
    <!-- Unique constraint (user, rol) -->
    <!-- ===================================================== -->

    <changeSet id="user-rol-002-unique-user-rol" author="system">
        <addUniqueConstraint
            tableName="user_rol"
            columnNames="user_id, rol_id"
            constraintName="uq_user_rol_user_rol"/>
    </changeSet>

    <!-- ===================================================== -->
    <!-- Índices -->
    <!-- ===================================================== -->

    <changeSet id="user-rol-003-index-is-deleted" author="system">
        <createIndex indexName="idx_user_rol_is_deleted" tableName="user_rol">
            <column name="is_deleted"/>
        </createIndex>
    </changeSet>

    <!-- ===================================================== -->
    <!-- Foreign Keys -->
    <!-- ===================================================== -->

    <changeSet id="user-rol-004-fk-user" author="system">
        <addForeignKeyConstraint
            baseTableName="user_rol"
            baseColumnNames="user_id"
            referencedTableName="user"
            referencedColumnNames="id"
            constraintName="fk_user_rol_user"
            onDelete="RESTRICT"/>
    </changeSet>

    <changeSet id="user-rol-005-fk-rol" author="system">
        <addForeignKeyConstraint
            baseTableName="user_rol"
            baseColumnNames="rol_id"
            referencedTableName="rol"
            referencedColumnNames="id"
            constraintName="fk_user_rol_rol"
            onDelete="RESTRICT"/>
    </changeSet>

    <changeSet id="user-rol-006-fk-createdby" author="system">
        <addForeignKeyConstraint
            baseTableName="user_rol"
            baseColumnNames="createdby_id"
            referencedTableName="user"
            referencedColumnNames="id"
            constraintName="fk_user_rol_createdby"/>
    </changeSet>

    <changeSet id="user-rol-007-fk-updatedby" author="system">
        <addForeignKeyConstraint
            baseTableName="user_rol"
            baseColumnNames="updatedby_id"
            referencedTableName="user"
            referencedColumnNames="id"
            constraintName="fk_user_rol_updatedby"/>
    </changeSet>

</databaseChangeLog>
